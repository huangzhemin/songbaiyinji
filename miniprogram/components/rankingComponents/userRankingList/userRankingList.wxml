<!--components/userRankingList/userRankingList.wxml-->
<block wx:for='{{userRankingList}}'  wx:for-index="idx" wx:key="idx">
  <pm-ranking-cell
    ranking='{{idx + 1}}'
    openId='{{item.openId}}'
    avatar='{{item.avatarUrl}}'
    nickName='{{item.nickName}}'
    points='{{item.points}}'
    jumpUrl='{{jumpUrl}}'>
  </pm-ranking-cell>
</block>

