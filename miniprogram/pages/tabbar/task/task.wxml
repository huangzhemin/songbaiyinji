<!--miniprogram/pages/tabbar/task/task.wxml-->
<van-tabs active="{{ active }}" bind:change="clickChange" swipeable sticky>
  <van-tab title="进行中" name="doing"></van-tab>
  <van-tab title="已结束" name="over"></van-tab>
</van-tabs>

 <swiper indicator-dots="{{indicatorDots}}"
               autoplay="{{autoplay}}" 
               interval="{{interval}}" 
               duration="{{duration}}"
               current="{{active}}"
               name="{{name}}"
               style="height:600px"
               bind:change="swipeChange" >
  <block wx:for="{{background}}" wx:key="*this">
    <swiper-item>
      <view class="swiper-item {{item}}">
        {{item.page}}
        <!-- <block wx:for='{{taskList}}'>
          <view style='height:30rpx; background-color:#92a8d1'></view>
        </block> -->
      </view>
    </swiper-item>
  </block>
</swiper>